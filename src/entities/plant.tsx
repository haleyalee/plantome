class Plant {
  constructor(public id: number, public name: string, public price: number,
      public category: string[], public quantity: number, public image: string, public _id?:number ) { }
}

export default Plant;